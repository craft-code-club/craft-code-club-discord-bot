name: Docker Build Test


on:
  workflow_dispatch:

  pull_request:
    types: [opened, reopened, synchronize]
    branches:
      - main


permissions: {}


env:
  IMAGE_NAME: ${{ github.repository_owner }}/${{ github.event.repository.name }}/cicd-test-build-image:validation-${{ github.run_number }}


jobs:
  test-docker-build:
    name: Test Docker Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@594f3bf4285d9ea8dc53c9a0c9c4092420091003 # v4

      - name: Build Docker image
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: ${{ env.IMAGE_NAME }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image was created
        run: |
          docker images
          if ! docker images --format "{{.Repository}}:{{.Tag}}" | grep -q "${{ env.IMAGE_NAME }}"; then
            echo "Error: Docker image was not created successfully"
            exit 1
          fi
          echo "Docker image verified successfully"
